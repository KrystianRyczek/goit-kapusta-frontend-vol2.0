import '../css/TransactionAddForm.css'

export default function TransactionAddForm({activeSheet}) {

  
  return (
    <div className='transaction-add-form-container'>
      <p>tansaction add form {activeSheet}</p>
    </div>
  );
}